package dnldd.backpack.utils;

public class TwitterKitUtils {
    /*protected static TwitterSession session;
    protected static TwitterAuthToken token;
    protected static TwitterKitUtils instance;
    protected static String userName;

    public static TwitterSession getTwitterSession(){ return session; }
    public static TwitterAuthToken getTwitterAuthToken(){ return token; }
    public static String getUserName(){ return userName; }

    protected TwitterKitUtils(){
        session = Twitter.getSessionManager().getActiveSession();
        token = session.getAuthToken();
        userName = Twitter.getSessionManager().getActiveSession().getUserName();
    }

    public static synchronized TwitterKitUtils getInstance(){
        if (instance == null) { instance = new TwitterKitUtils(); }
        return instance;
    }

    public static void logOut(){ Twitter.logOut(); }

    public static void logIn(Activity activity){ Twitter.logIn(activity, new Callback<TwitterSession>() {
        @Override
        public void success(Result<TwitterSession> twitterSessionResult) { TwitterKitUtils.getInstance(); }

        @Override
        public void failure(TwitterException e) {}
    }); }*/
}
